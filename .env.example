# Copia questo file in .env e sostituisci i valori con quelli reali.
# NON committare mai .env su Git — contiene segreti e credenziali.
# Per generare una SECRET_KEY sicura esegui:
#   python3 -c "import secrets; print(secrets.token_hex(32))"

SECRET_KEY=
DATABASE_URI=
ANTHROPIC_API_KEY=
FLASK_ENV=